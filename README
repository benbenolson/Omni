To build, I use the following command:
```
./setupOmni --enable-devicecompile
```

If you want to build some PPD files for your printer, you can use:
```
./GenerateOmniPPDs --buildOnly
```

Consult the `docs/` directory for further documentation.
